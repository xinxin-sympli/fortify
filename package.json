{
  "name": "fortify-landing",
  "version": "1.1.0",
  "description": "Fortify enables web applications to use smart cards, local certificate stores and do certificate enrollment.",
  "main": "index.js",
  "scripts": {
    "build:css": "sass src/styles/app.sass:src/styles/app.css --no-source-map",
    "predeploy:gh-pages": "npm run build:css",
    "deploy:gh-pages": "gh-pages -d src"
  },
  "author": "https://github.com/PeculiarVentures",
  "license": "MIT",
  "devDependencies": {
    "gh-pages": "^2.2.0",
    "sass": "^1.25.0"
  }
}
